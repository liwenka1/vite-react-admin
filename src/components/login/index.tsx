import React from 'react'
import { Button, Space } from 'antd'

const Login: React.FC = () => (
  <Space direction="vertical" style={{ float: 'right', marginRight: 15 }}>
    <Button>登录</Button>
  </Space>
)

export default Login
